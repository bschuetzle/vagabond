
<div class="container">
  <h1>Edit your Post</h1>
  <%= form_for ([@location, @post]), :html => {:class => 'form_for'} do |f| %>
    <%= f.text_field :title %>
    <%= f.text_area :body, rows: 5 %>
    <%= f.submit "Save Changes", class: 'default_button' %>
  <% end %>
</div>
