<div class="container">
  <div class="all_locations">
    <h1 class="form_header_text">All Cities</h1>
    <% @locations.each do |location| %>
    <div class="loc_loc">
      <h4> <%= link_to(location) do %>
      <%= location.city %>
      <% end %>
    </h4>
    </div>
    <% end %>
  </div>
</div>
