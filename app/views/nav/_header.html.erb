
<nav class="navbar navbar-default hey">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <p class="navbar-brand"><%= link_to "InterRail", root_path %></p>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right ">
        <% if current_user %>
        <li><%= link_to "What's up, " + current_user.name.split(" ")[0] + "?", user_path(current_user.friendly_id) %></li>
        <li class="dropdown">
          <a class="dropdown-toggle fa fa-train" data-toggle="dropdown" href="#"><span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to "View Cities", locations_path %></li>
            <li><%= link_to "Edit Profile", edit_user_path(current_user.friendly_id) %></li>
            <li role="separator" class="divider"></li>
            <li><%= link_to "Log Out", logout_path, method: "delete" %></li>
          </ul>
        </li>
        <% else %>
        <li><%= link_to "Log In", login_path %></li>
        <li><%= link_to "Sign Up", signup_path %></li>
        <li class="dropdown">
          <a class="dropdown-toggle fa fa-train" data-toggle="dropdown" href="#"><span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to "View Posted Cities", locations_path %></li>

          </ul>
        <% end %>
      </ul>
    </li>
  </div><!-- /.navbar-collapse -->
</div><!-- /.container-fluid -->
</nav>
